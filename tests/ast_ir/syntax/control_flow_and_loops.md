# Checklist

- AST contains `WhileAST`, `IfAST`, nested `BlockAST`, empty statement block, and range `ForAST`.
- IR shows loop back-edges and conditional branches for `if/else` and `while`.
- No semantic/type diagnostics expected.
