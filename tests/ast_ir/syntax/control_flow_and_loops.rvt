fun int main() {
    int sum = 0;
    int i = 0;

    while (i < 5) {
        if (i == 2) {
            ;
        } else {
            sum = sum + i;
        }
        i = i + 1;
    }

    for (j in 0 to 4 step 1) {
        sum = sum + j;
    }

    return sum;
}
