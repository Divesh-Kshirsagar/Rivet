# Checklist

- AST binary tree shape reflects operator precedence (`*` before `+`, logical precedence, shifts).
- Parenthesized expression changes tree shape for `b`.
- IR arithmetic/compare operations appear in expected grouping order.
