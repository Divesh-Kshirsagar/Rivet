# Checklist

- `address_of`, `deref`, `ref`, `optref`, and `null` interaction should pass.
- AST should contain `AddressOfAST`, `DerefAST`, and null literal usage.
- IR should show pointer-like load/store lowering.
